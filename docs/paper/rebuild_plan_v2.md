# Что пересобрать под статью в ГИиП — ревизия от 06.08.2026

Ревизия плана `paper_skeleton_and_gaps.md` (19.07.2026) с учётом того, что
M1/M2/M3/M5 уже посчитаны, 33f-модель Красноярска обучена, а постпроцессинг
**выведен из статьи** (уходит в отдельную публикацию).

Статус источников: этот файл написан по содержимому репозитория и манифестам
прогонов. Сами `.log`, `.npz` и `.pth` в репозитории отсутствуют (см. Б-1).

---

## 0. Журнал: решение подтверждено

**«Гидрометеорологические исследования и прогнозы»** (Гидрометцентр России).
Решение из `journal_research.md` от 19.07.2026 остаётся в силе и после
исключения постпроцессинга. Перепроверено 06.08.2026:

- **Белый список, 1-й уровень (У1)** — присвоен в октябре 2025, до этого журнал
  был на 3-м уровне (новость Гидрометцентра от 05.10.2025).
- **ВАК К1** — указан на странице журнала (`meteoinfo.ru/proceedings`).
- Публикация **бесплатная**, авторский экземпляр — по запросу.
- Подача: `zaimsky@mail.ru` и `onti@mecom.ru`, заявка по форме (DOC с сайта).
- Лимит **20 стр.** включая всё, TNR 12, интервал 1.5 — из PDF «Правила для
  авторов»; **скачать и сверить перед подачей**, в HTML-версии сайта лимита нет.

Исключение постпроцессинга выбор не меняет, а укрепляет: статья ужимается до
~15–17 стр. и уходит от лимита с запасом. Альтернативу «Метеорология и
гидрология» это по-прежнему не открывает — там 18 стр. через **два** интервала
и **не более 3 рисунков**, а в текущем скелете рисунков 7–9.

---

## Б. БЛОКЕРЫ — устранить раньше всего остального

### Б-1. Первичные данные всех опубликованных чисел отсутствуют в репозитории

Коммит `710fd29` называется «version run logs … and per-sample metrics», но
по факту добавил **только три MANIFEST.md**: `.gitignore` молча отсёк `*.log`
(строка 62), `*.npz` (192) и `/vm_backup/` (219).

Следствие: ни одно число в тексте статьи сейчас нельзя проследить до расчёта.
Это ровно тот дефект провенанса, ради устранения которого затевался пересчёт.

**Что делать:** забрать с VM `/workdir/paper_results/*.log` и `*_samples.npz`,
положить в репозиторий с `git add -f` (логи текстовые, десятки КБ). Без этого
не закрываются Б-2, Т0-3 и Т0-4.

### Б-2. Раздел 6 описывает не ту модель, которая работает в оперативе

Текст: «Модель, описанная в разделах 3–4, доведена до автоматического выпуска
прогноза». Фактически единственный оперативный контур —
`scripts/cron_forecast_russia_v3.sh` — запускает:

- `experiments/multires_russia_33f_v3_noroi` — модель по **всей России**, **33
  канала**, обученная на другом датасете;
- плюс `experiments/neural_postproc_v3` — **нейросетевой постпроцессинг**,
  который из статьи исключён.

То есть в оперативе работает не флагман разделов 3–4
(`multires_merge_freeze6_v2`, Красноярск, 19 каналов) — другой домен, другой
состав каналов, другая обучающая сборка. Красноярского оперативного крона в
репозитории нет вообще.

Это тот же класс дефекта, что П-2/П-3/П-11 карты проблем (ложная атрибуция
модели), и он в самом уязвимом разделе. Рецензент Гидрометцентра проверяет
оперативные заявления в первую очередь.

**Три честных выхода, выбрать один:**

1. Переписать раздел 6 как «оперативная апробация подхода на модели по России»
   с явным указанием, что это другая конфигурация, — тогда придётся описать и
   постпроцессинг, что противоречит решению вынести его в отдельную статью.
2. Поднять оперативный контур на красноярском флагмане (GDAS → merge-сетка →
   `multires_merge_freeze6_v2`) и накопить серию. Честнее всего, но это недели
   календарного времени.
3. Снять раздел 6, оставив одну фразу в разделе 7: подход доведён до
   автоматического выпуска в родственной конфигурации, оперативная верификация —
   предмет отдельной работы. Дёшево, снимает риск, освобождает ~1 стр.

**Рекомендация — вариант 3** для первой подачи, вариант 2 как задел на вторую.
Заявление про «оперативный контур» стоит убрать и из аннотации, и из выводов
(п. 6), иначе аннотация обещает то, чего в статье нет.

---

## Т0. Бесплатно, без GPU — числа уже посчитаны, надо перенести

**Т0-1. Календарные границы выборок — ЗАКРЫТО, вписывать в текст.**
Проверено арифметически: `n_time = 16072`, окно `2+4`, отсюда
`16072 − 6 + 1 = 16067` примеров — ровно столько, сколько в тексте, значит
датасет одночанковый и индекс отображается в дату линейно
(индекс 0 = 01.01.2010 00 UTC, шаг 6 ч). `test_fraction = 0.2` →
12853 / 1607 / 1607, что совпадает с текстом.

| Выборка | Примеров | Срок t₀ (последний входной срез) | Последний таргет |
|---|---:|---|---|
| Обучающая | 12 853 | 01.01.2010 06 UTC – 19.10.2018 06 UTC | 20.10.2018 06 UTC |
| Проверочная | 1 607 | 19.10.2018 12 UTC – 25.11.2019 00 UTC | 26.11.2019 00 UTC |
| Тестовая | 1 607 | 25.11.2019 06 UTC – 30.12.2020 18 UTC | 31.12.2020 18 UTC |

Совпадает с прежней оценкой «~25.11.2019–31.12.2020». Тестовое окно — 13 месяцев,
полный годовой цикл, утверждение «все четыре сезона» корректно.

**Т0-2. Число прогностических каналов** в формуле (3): **C = 17** (19 полей
минус `z_surf` и `lsm`).

**Т0-3. Табл. 5.3, ячейки +96/+120/+168 для колонок «первые 4 шага» и «ОИ 1 %».**
Пересчёт НЕ нужен: прогоны `m1_oi10_first4_ar28` и `m1_oi1_ar28` выполнены,
логи по 20 КБ содержат полную поканальную таблицу по всем 28 горизонтам. В
статью перенесены только первые строки. Достаточно забрать логи (Б-1).

**Т0-4. Таблицы усвоения (сейчас два `{{ЗАПОЛНИТЬ}}` в п. 5.6).**
Все числа есть в манифесте `vm3_m2m3`:

- свип на val (12 конфигураций): максимум skill **86.55 % при L = 100 км,
  σ_o = 0.5** — это и есть конфигурация, применённая на тесте, протокол
  «подбор на val → отчёт на test» соблюдён и его надо показать;
- test: baseline 70.26 %; нуджинг α = 0.5/0.7/0.9 → 71.79/72.10/**72.29** %;
  ОИ 1 % → 77.24 %; ОИ 10 % по трём расстановкам станций → 84.65/84.89/85.08 %
  (среднее 84.87 %, разброс 0.43 п.п. — разброс мал, это стоит указать).

Замечание: в тексте раздела 5 сказано «лучший нуджинг α = 0.7»; по факту
монотонно лучший — α = 0.9. Исправить.

**Т0-5. Поканальная декомпозиция расхождения агрегата (8.7 п.п.).**
Считается из `docs/paper/tables_m2m3.md` без новых прогонов. Видно уже сейчас:
разница флагмана и `freeze6` по t2m/ветру/z нулевая либо в пользу `freeze6`,
значит агрегат вытянут каналами, которых нет в поканальной таблице
(`tp`, `sp`, `tcwv`, `q@850`, `q@500`). Это и есть ответ на вопрос «на чём
формируется разница» — и он усиливает тезис о недостаточности агрегата.
Нужны per-channel строки по этим пяти каналам из логов M2.

**Т0-6. Доверительные интервалы для Табл. 1.** `scripts/paper_bootstrap_ci.py`
готов, нужны `*_samples.npz` (Б-1). GPU не требуется.

---

## Т1. Обязательный счёт

**Т1-1. M4 — абляционная матрица «зачем нужен единый граф». Единственный MUST
из старого плана, который так и не выполнен.** Без него не отвечен первый
вопрос любого рецензента: что даёт вставка 0.25° по сравнению с интерполяцией
глобального прогноза. Строки: инерция | климатология | глобальная v2 +
билинейная интерполяция в ROI | региональная GNN standalone | мультимасштабная.
Команды готовы (`vm_commands.md`, этап 5), стоимость < 1 GPU-ч после сборки.
Риск, который надо принять заранее: если по t2m мультимасштаб ≈ интерполяция,
показываем каналы, где выигрыш есть (ветер, msl), и формулируем сдержанно.

**Т1-2. Оценка 33f (19 против 33 каналов).** Раннер
`scripts/_paper_run_33f_eval.sh` написан, модель обучена (состояния: эпоха 15 —
лучшая по val, эпоха 29 — стадия AR=4), но результатов в репозитории нет.
Либо забрать с VM, либо запустить (~2–3 GPU-ч).

**Т1-3. Климатология ROI (S2)** — второй эталон. Заявлен в разделе 4 и в
ограничениях п. 4; если строки не будет, упоминание надо вычистить из обоих
мест. CPU, часы.

---

## Т2. Сильно усилит — по бюджету

- **S1. Диагностика стыка** (карта t2m через границу вставки + RMSE по бинам
  расстояния). Тезис «бесшовность» сейчас заявлен, но ничем не подтверждён —
  дыра в главном методическом утверждении. Нужны сохранённые предсказания M2.
- **S3. Сравнение с IFS HRES** из WeatherBench2. Отсутствие сравнения с
  оперативной моделью — самое предсказуемое замечание рецензента-практика;
  в `journal_research.md` это прямо предсказано.
- **S4. Верификация по станциям ISD-Lite** в ROI.

---

## Решения, которые надо принять до написания текста

**Р-1. 19f или 33f как основная линия.** Вся серия усвоения (M1, M5) и вся
абляция (M2, M3) посчитаны на 19-канальном флагмане. Перевод статьи на 33f
означает пересчёт всего. **Рекомендация: оставить 19f основной линией**, а 33f
дать отдельным подпунктом «расширение состава прогностических полей» — ровно
так, как уже написано в разделе 4 («в-третьих, состав каналов»).

Отдельно: расхождение диплома («описаны уровни, которых в v2 нет») в тексте
статьи **уже исправлено** — раздел 2 честно перечисляет 19 полей с уровнями
850 и 500 гПа. Это не долг, а закрытый пункт.

**Р-2. Судьба раздела 6** — см. Б-2. Решение влияет на аннотацию и выводы.

**Р-3. Аннотация.** Содержит «успешность 69 %» — это агрегат по 17 каналам, а
не по приземной температуре, тогда как в той же фразе рядом стоит RMSE t2m.
Читается как «69 % по температуре». Переформулировать.

---

## Порядок работ

1. Б-1 (забрать логи и npz) — разблокирует Т0-3, Т0-4, Т0-5, Т0-6.
2. Т0-1…Т0-6 — текстовые правки без счёта, закрывают около половины плейсхолдеров.
3. Решения Р-1, Р-2, Р-3.
4. Т1-1 (M4) и Т1-2 (33f) — счёт.
5. Т1-3, затем Т2 по остатку бюджета.
6. Сверка каждой публикуемой цифры с логом провенанса, оформление под журнал.
