# 5. Усвоение наблюдений в цикле прогноза

_(оценка: 2.5 стр.)_

## 5.1. Постановка задачи

Как показано в п. 4.5, ошибка авторегрессионного прогноза монотонно нарастает с горизонтом. Естественный способ ограничить этот рост — периодически корректировать прогностическое состояние по фактическим измерениям, то есть выполнять усвоение данных.

Необходимость специальной процедуры вытекает из несоответствия двух представлений информации. Модели требуется значение каждой прогностической переменной **во всех** $N = 133\,279$ узлах расчётной сетки; наблюдения же доступны в отдельных точках, покрывают лишь часть переменных и распределены крайне неравномерно. Прямая подстановка наблюдений вместо прогноза невозможна: она оставила бы неопределёнными подавляющее большинство узлов и нарушила бы согласованность полей. Задача усвоения — совместить полноту прогностического поля с точностью измерений, распространив информацию из точек наблюдений на окрестность физически осмысленным образом [1].

Здесь исследуются два классических метода: релаксационное усвоение (нуджинг) и оптимальная интерполяция. Оба применяются **пошагово внутри авторегрессионной развёртки**: после каждого шага прогноза (3) поле корректируется по наблюдениям, и уже исправленное состояние подаётся на вход следующего шага.

## 5.2. Релаксационное усвоение

Простейшая схема коррекции — линейная релаксация прогностического поля к наблюдениям в тех узлах, где они доступны:

$$x^{a}_{g} \;=\; x^{b}_{g} \;+\; \alpha \, m_g \left( y_g - x^{b}_{g} \right), \tag{1}$$

где $x^{b}_{g}$ — прогностическое (фоновое) значение в узле $g$; $y_g$ — наблюдение; $m_g \in \{0, 1\}$ — индикатор наличия наблюдения в узле; $\alpha \in (0, 1]$ — коэффициент релаксации; $x^{a}_{g}$ — скорректированное значение (анализ).

Схема (1) вычислительно тривиальна, но обладает существенным недостатком: коррекция применяется **только** в наблюдаемых узлах, информация не распространяется на соседние. При разрежённой сети это приводит к появлению локальных возмущений, не согласованных с окружающим полем.

## 5.3. Оптимальная интерполяция

Оптимальная интерполяция [1] учитывает пространственную структуру ошибок и потому распространяет поправку на окрестность каждого наблюдения. Анализ вычисляется как

$$\mathbf{x}^{a} \;=\; \mathbf{x}^{b} \;+\; \mathbf{K}\left( \mathbf{y} - \mathbf{H}\mathbf{x}^{b} \right), \qquad
\mathbf{K} \;=\; \mathbf{B}\mathbf{H}^{\mathsf{T}} \left( \mathbf{H}\mathbf{B}\mathbf{H}^{\mathsf{T}} + \mathbf{R} \right)^{-1}, \tag{2}$$

где $\mathbf{x}^{b}$ — вектор фонового (прогностического) состояния; $\mathbf{y}$ — вектор наблюдений; $\mathbf{H}$ — оператор наблюдений, сопоставляющий состоянию модели значения в точках измерений; $\mathbf{B}$ — ковариационная матрица ошибок фона; $\mathbf{R}$ — ковариационная матрица ошибок наблюдений; $\mathbf{K}$ — матрица усиления.

Ковариации ошибок фона задаются гауссовой моделью, убывающей с расстоянием:

$$B_{ij} \;=\; \sigma_b^{2} \exp\!\left( -\frac{d_{ij}^{2}}{L^{2}} \right), \tag{3}$$

где $d_{ij}$ — расстояние между узлами $i$ и $j$, вычисляемое по формуле гаверсинуса; $L$ — радиус корреляции; $\sigma_b$ — среднеквадратическое отклонение ошибки фона. Ошибки наблюдений полагаются некоррелированными и одинаковыми по величине: $\mathbf{R} = \sigma_o^{2} \mathbf{I}$, где $\sigma_o$ — среднеквадратическое отклонение ошибки наблюдения.

Оператор $\mathbf{H}$ реализован как выбор ближайшего узла расчётной сетки к точке наблюдения. Усвоение выполняется независимо для каждой прогностической переменной; матрица $\mathbf{B}$ строится однократно при инициализации и повторно используется на всех шагах.

Отметим свойство схемы, существенное для настройки: подстановка $\mathbf{B} = \sigma_b^{2}\mathbf{C}$ и $\mathbf{R} = \sigma_o^{2}\mathbf{I}$ в (2) даёт

$$\mathbf{K} \;=\; \mathbf{C}\mathbf{H}^{\mathsf{T}} \left( \mathbf{H}\mathbf{C}\mathbf{H}^{\mathsf{T}} + \frac{\sigma_o^{2}}{\sigma_b^{2}} \mathbf{I} \right)^{-1}, \tag{4}$$

то есть матрица усиления зависит не от $\sigma_b$ и $\sigma_o$ по отдельности, а только от их **отношения**. Поэтому в экспериментах величина $\sigma_b = 0.8$ фиксирована, а подбирается $\sigma_o$; этого достаточно для перебора всех различимых конфигураций схемы.

## 5.4. Обоснование выбора методов

Современные оперативные системы усвоения используют, как правило, вариационные методы (в том числе четырёхмерный вариационный анализ) либо ансамблевые фильтры Калмана и их гибриды, превосходящие оптимальную интерполяцию по точности [1]. Выбор в пользу более простых схем в настоящей работе продиктован постановкой задачи.

Во-первых, целью является не построение оптимальной системы усвоения, а **проверка принципиальной совместимости** пошаговой коррекции с авторегрессионным нейросетевым прогнозом: прогностический оператор здесь — обученная нейронная сеть, а не численное интегрирование уравнений, и поведение цикла «прогноз — коррекция» в таком сочетании заранее неочевидно.

Во-вторых, вариационные методы требуют построения сопряжённой модели, а ансамблевые — многократного прогона прогностической модели для формирования ансамбля; и то, и другое представляет собой самостоятельную задачу, выходящую за рамки данной работы.

В-третьих, схемы (1) и (2) вычислительно пренебрежимы на фоне самого прогноза и не нарушают основного свойства подхода — выпуска прогноза за секунды на одном ускорителе.

Отметим направление, естественное именно для нейросетевой постановки: прогностический оператор дифференцируем, поэтому градиент функционала невязки по начальному состоянию доступен средствами автоматического дифференцирования. Это открывает путь к вариационному усвоению без построения сопряжённой модели вручную; такая работа планируется как продолжение.

## 5.5. Постановка эксперимента

Эксперименты выполнены по схеме «идентичных близнецов» (identical twin): в качестве наблюдений используются значения реанализа ERA5 в случайно выбранном подмножестве узлов региональной вставки. Рассмотрены две плотности сети: 10 % узлов (около 250 условных станций) и 1 % (около 25 условных станций); вторая ближе к реальной плотности наземной сети в регионе.

Такая постановка является **идеализацией** и это следует оговорить прямо: наблюдения не содержат инструментальной погрешности и ошибки репрезентативности, размещены строго в узлах расчётной сетки и доступны для всех прогностических переменных одновременно. Следовательно, полученные величины прироста качества характеризуют **верхнюю границу** возможного эффекта, а не ожидаемый результат при усвоении реальных измерений. Оценка на фактических станционных данных — предмет дальнейшей работы (п. 7).

Гиперпараметры схемы ($L$ и $\sigma_o$) подбирались **на проверочной выборке**, а результаты приводятся на тестовой; для схемы (1) аналогично подбирался коэффициент $\alpha$. Чтобы результат не зависел от конкретной случайной реализации сети наблюдений, прогоны на тестовой выборке повторены с тремя различными расстановками условных станций.

## 5.6. Результаты

{{ЗАПОЛНИТЬ: табл. — подбор гиперпараметров ОИ на проверочной выборке: RMSE приземной температуры и успешность для сочетаний L ∈ {50, 100, 150, 200} км и σ_o ∈ {0.3, 0.5, 1.0}, плотность наблюдений 10 %, горизонты +6…+24 ч; указать выбранную конфигурацию}}

{{ЗАПОЛНИТЬ: табл. — результаты на тестовой выборке: без усвоения / нуджинг (α = 0.5, 0.7, 0.9) / оптимальная интерполяция при 10 % и 1 % наблюдений; RMSE приземной температуры и успешность по горизонтам +6…+24 ч; для ОИ при 10 % — среднее по трём расстановкам станций с указанием разброса}}

{{ЗАПОЛНИТЬ: сопоставление двух схем — величина превосходства оптимальной интерполяции над релаксационным усвоением в пунктах успешности; ожидается, что превосходство обусловлено распространением поправки на окрестность}}

## 5.7. Усвоение на длинных горизонтах

Мотивировка усвоения (п. 5.1) опирается на деградацию прогноза при длительной авторегрессионной развёртке, тогда как приведённые выше оценки ограничены сроком +24 ч. Чтобы устранить это несоответствие, выполнена отдельная серия расчётов с развёрткой на 28 шагов (168 ч) в четырёх режимах: без усвоения; с оптимальной интерполяцией на каждом шаге при плотности наблюдений 10 %; с усвоением только на первых четырёх шагах развёртки; с усвоением на каждом шаге при плотности 1 %. Оценка выполнена на 200 сроках начала прогноза из тестовой выборки, область — региональная вставка (2501 узел).

**Табл. 5.3.** Среднеквадратическая ошибка прогноза приземной температуры (°C) и успешность относительно инерционного прогноза (%) при развёртке до 168 ч. Основная конфигурация модели, 200 сроков.

| Горизонт | без усвоения | ОИ, 10 % | ОИ, 10 %, только первые 4 шага | ОИ, 1 % |
|---|---:|---:|---:|---:|
| +6 ч | 1.44 | 0.84 | 0.84 | 1.22 |
| +24 ч | 2.11 | 1.12 | 1.12 | 1.74 |
| +30 ч | 2.29 | 1.21 | 1.70 | 1.87 |
| +48 ч | 2.59 | 1.31 | 2.36 | 2.05 |
| +96 ч | 4.07 | 1.57 | {{ЗАПОЛНИТЬ}} | {{ЗАПОЛНИТЬ}} |
| +120 ч | 5.06 | 1.72 | {{ЗАПОЛНИТЬ}} | {{ЗАПОЛНИТЬ}} |
| +168 ч | 7.31 | 2.23 | {{ЗАПОЛНИТЬ}} | {{ЗАПОЛНИТЬ}} |
| Успешность на +168 ч, % | 18.9 | 78.3 | 18.7 | 68.9 |

Из табл. 5.3 следуют три вывода.

**Первое.** Без усвоения ошибка растёт монотонно: от 2.11 °C на суточном сроке до 7.31 °C на семисуточном, то есть в 3.5 раза. Успешность относительно инерционного прогноза максимальна около +18…+24 ч (73 %) и затем плавно убывает до 19 % на +168 ч. Резкого обрыва («коллапса») в основной конфигурации не наблюдается — деградация постепенная; вместе с тем ошибка 4 °C и более на сроках свыше четырёх суток делает прогноз малопригодным для практического использования.

**Второе.** Непрерывное усвоение принципиально меняет характер деградации: ошибка возрастает лишь вдвое (с 1.12 до 2.23 °C), а успешность держится в диапазоне 78–86 % на всех горизонтах. Показательно, что ошибка на семисуточном сроке с усвоением (2.23 °C) примерно равна ошибке суточного прогноза без усвоения (2.11 °C). Механизм понятен: коррекция на каждом шаге частично устраняет накопленную погрешность, и авторегрессия перестаёт быть источником неограниченного роста ошибки. Различие статистически значимо на всех горизонтах: блочный бутстреп (блок 5 суток, 2000 реплик, парное сравнение на совпадающих сроках) даёт для выигрыша 95 %-й доверительный интервал [0.89; 1.05] °C на +24 ч и [4.28; 6.29] °C на +168 ч.

**Третье, и наиболее существенное для практики.** Если усвоение прекращается, эффект сохраняется недолго. В режиме коррекции только на первых четырёх шагах ошибка совпадает с непрерывным усвоением до +24 ч, но уже через шесть часов после прекращения коррекции преимущество сокращается (1.70 против 1.21 °C), а спустя сутки практически исчезает (2.36 против 2.59 °C в прогоне без усвоения). К семисуточному сроку успешность (18.7 %) совпадает с прогоном вообще без усвоения (18.9 %). Иными словами, «память» модели о единичной коррекции начального состояния составляет порядка суток, и улучшение качества требует **регулярного** поступления наблюдений, а не однократной инициализации. Именно этим обосновано устройство оперативной схемы (раздел 6), в которой прогноз перезапускается по свежему анализу каждые шесть часов.

Снижение плотности наблюдений в десять раз (с 10 % до 1 % узлов, то есть примерно с 250 до 25 условных станций, что ближе к реальной плотности наземной сети) ослабляет эффект, но не устраняет его: ошибка на суточном сроке составляет 1.74 °C против 2.11 °C без усвоения, а успешность на +168 ч сохраняется на уровне 68.9 % против 18.9 %. Это указывает на устойчивость вывода к разрежённости сети.

## 5.8. Устойчивость конфигураций при длительной развёртке

Отдельного внимания заслуживает поведение конфигурации без остаточной формулировки (конфигурация 2 из п. 4.2) при длительной развёртке. До суточного срока она практически не отличается от основной (2.12 против 2.11 °C на +24 ч), однако на дальних сроках теряет устойчивость: ошибка достигает 8.12 °C на +120 ч и 15.62 °C на +168 ч против 5.06 и 7.31 °C у основной конфигурации. Успешность относительно инерционного прогноза обращается в ноль около +126 ч и далее становится отрицательной (до −91 % на +168 ч), то есть прогноз оказывается хуже сохранения последнего анализа без изменений.

Дополнительный признак неустойчивости — немонотонность ошибки по горизонтам: 3.96 → 4.63 → 4.46 → 5.54 → 5.08 → 6.65 °C на сроках +78…+108 ч, тогда как у основной конфигурации рост строго монотонен. Такая пилообразность характерна для раскачки в авторегрессионном режиме.

Отсюда следует, что предсказание приращения поля (остаточная формулировка) существенно для устойчивости при длительной развёртке, тогда как на сроках до суток его влияние незначительно. Это уточняет формулировку, приводившуюся ранее в наших материалах: быстрая потеря качества после четырёх суток характерна не для подхода в целом, а для конфигурации без остаточной формулировки.

{{ЗАПОЛНИТЬ: результат прогона конфигурации 2 с усвоением на 28 шагах — предотвращает ли усвоение раскачку; если да, это дополнительный аргумент в пользу усвоения как стабилизирующего механизма}}

---

## Рисунки

- **Рис. (усвоение-1).** Схема пошагового усвоения в авторегрессионном цикле: прогноз на 6 ч → коррекция по наблюдениям → следующий шаг.
- **Рис. (усвоение-2).** Поле поправки оптимальной интерполяции: положение условных станций и распространение поправки на окрестность при $L = 100$ км (иллюстрация к (3)).
- **Рис. (усвоение-3).** RMSE приземной температуры до +168 ч для четырёх режимов усвоения (п. 5.7).

## Таблицы

- **Табл. (усвоение-1).** Подбор $L$ и $\sigma_o$ на проверочной выборке (12 сочетаний).
- **Табл. (усвоение-2).** Результаты на тестовой выборке: без усвоения, нуджинг, оптимальная интерполяция при 10 % и 1 % наблюдений; по горизонтам.

## Литература (локальная нумерация)

1. Гандин Л. С. Объективный анализ метеорологических полей. Л.: Гидрометеоиздат, 1963. 286 с.
2. Kalnay E. Atmospheric Modeling, Data Assimilation and Predictability. Cambridge: Cambridge University Press, 2003. 341 p.
3. {{ЗАПОЛНИТЬ: 1–2 ссылки на современные работы по усвоению данных в нейросетевых моделях прогноза (например, обучаемые схемы усвоения или гибридные подходы), желательно 2024–2026 гг.}}

## Замечания автору

- Все числовые результаты раздела — плейсхолдеры: соответствующие расчёты (серия M1 для длинных горизонтов и серия M5 для протокола подбора гиперпараметров) выполняются. Прежние числа из дипломной работы **сознательно не перенесены**: они получены на другой обучающей сборке и на выборке из ~200 сроков одного холодного периода, тогда как в статье принято единое окно верификации (1607 сроков, все сезоны).
- Формулировка «около 250 условных станций» получена как 10 % от 2501 узла вставки; при желании заменить на точное число узлов, использованных в расчёте.
- Оговорка об идеализации постановки (п. 5.5) добавлена намеренно и до появления чисел: она предупреждает основное возражение рецензента. Прошу не смягчать её при вычитке.
- Если по итогам расчётов эффект усвоения на длинных горизонтах окажется слабым, п. 5.7 следует переформулировать как ограничение метода, а не удалять — отрицательный результат здесь содержателен и объясняет, почему в оперативной схеме прогноз перезапускается по свежему анализу каждые 6 ч.
- Абзац о дифференцируемости прогностического оператора (п. 5.4) связывает раздел с направлениями дальнейшей работы (п. 7); при сокращении объёма его можно перенести целиком в раздел 7.
- Сверить обозначения с разделом 3: там оператор прогноза обозначен $\mathcal{F}$, а состояние — $x$; в настоящем разделе используются $\mathbf{x}^{b}$ и $\mathbf{x}^{a}$. Нужна единая система обозначений по всей статье.
