# Ответы по правкам диплома (новая итерация)

Файл с дипломом перенесён из корня в `docs/diploma/`. Пройдён по всему тексту, не только по перечисленным пунктам — где нашёл аналогичные проблемы, поправил без отдельного запроса. PDF пересобран (53 стр., 3 прохода XeLaTeX).

Ниже — ответ по каждому замечанию.

---

## 1. Файлы диплома в корне репозитория
Перенесены: `diploma.tex` → `docs/diploma/diploma.tex`, ревью → `docs/diploma/DIPLOMA_REVIEW_ANSWERS.md`. PDF/`.aux`/`.log`/`.toc` теперь генерируются только в `docs/diploma/`. Корень от LaTeX-артефактов очищен.

## 2. Раздел 3.1 — расхождение по разрешению (0.25° vs 0.7°)
Исправлено. В разделе явно прописано: исходные данные ERA5 0.25°, для глобальной модели субдискретизируются до 512×256 (~0.7°), для регионального дообучения берётся фрагмент 61×41 с шагом 0.25°.

## 3. Жирное выделение терминов в теле текста
Удалено повсеместно. Жирным теперь оформлены только:
- подписи таблиц («**Таблица N.**»),
- лучшее значение в строке/столбце,
- структурные маркеры заголовков пунктов.

Удалены жирные начертания на: «Энкодер/Процессор/Декодер», «Цикл усвоения данных», «фоновое поле/наблюдения», «representativeness error», «Urban Heat Island», «Model Output Statistics», «HistGradientBoostingRegressor», «Inverse Distance Weighting», «Параметры:», «Реализация.», «Скорость и направление:», «Тип осадков», «каскадная схема», «поправка», «селективно», «мультипликативная сезонная коррекция» и т. д.

## 4. Английские дубликаты в скобках
Удалены: «Процессор (Processor)», «Энкодер (Encoder)», «Декодер (Decoder)», «(ОИ)», «корректирующая поправка (corrective residual)», «(representativeness error)», «(Urban Heat Island, UHI)» и т. п. Если термин используется только на английском (Skill Score, Anomaly Correlation Coefficient, MOS, IDW, Nudging) — остался без перевода в скобках.

## 5. «Fine-tune» / «файнтюн»
Везде заменено на «дообучение»: в разделах 3.5, 7.1, 7.4, 7.5, 8 и в заключении. В коде/конфигах термин не правил (это уже инфраструктура).

## 6. Стрелки `→` как маркер AI-стиля
Все «слоп»-стрелки в прозе заменены на нормальные русские конструкции:
- «1.67 → 1.69 °C» → «с 1.67 °C до 1.69 °C»;
- пайплайн постпроцессинга `GNN → lapse → MOS → IDW` переписан прозой (перечислением);
- «78.20 → 85.81 %» → «с 78.20 % до 85.81 %»;
- «UHI + температурные инверсии» → «вклад городского острова и температурных инверсий».

Стрелки в формулах (`\to` в архитектуре UNet `64→128→256→512`) сохранены — это нормальная нотация в формуле.

## 7. Чекпоинты в названиях и тексте (`multires_nores_freeze6`, `multires_merge_freeze6_v2` и т. п.)
Удалены из читаемого текста разделов 7.4, 7.5, 7.6 и заключения. Где требовалось — заменены на нейтральные описания («базовый чекпоинт freeze6», «модель, дообученная на merge-датасете»). Подзаголовки тоже переименованы: «Дообучение на merge-датасете с остаточной связью» вместо `multires_merge_freeze6_v2`.

## 8. Конфигурационные параметры в прозе (`use_residual=true`, `freeze_processor_epochs=10`, `finetune_processor_lr_factor=0.05`)
Удалены. Описаны словами: «предсказывается приращение, а не абсолютное поле», «длина фазы заморозки увеличена с 6 до 10 эпох», «снижен локальный темп обучения процессора в 10 раз», «базовый темп обучения 5·10⁻⁵».

## 9. Раздел 7.2 — режимы `interpolate` и `merge`
Полностью убран `interpolate`-режим из основного текста: пользователь явно сказал «убрать нах». В разделе 7.2 остался только режим `merge` с пояснением, что регион получает реальные данные ERA5 0.25°, а не интерполированный из 0.7° грид. В разделе 7.6 удалены парные сравнения interp vs merge.

## 10. Раздел 7.6 — таблицы 8a/8b/8c/8d
Структура переработана:
- бывшие таблицы 8a (interp) и 8b (merge) объединены в одну **Таблицу 8a** только по merge-датасету;
- таблица 8b (теперь) — зависимость Skill от состава усваиваемых переменных (только merge);
- таблица 8c (теперь) — Skill по горизонтам прогноза +6…+24 ч (только merge);
- таблица 8d (бывшая 8e) — серия DA на глобальной сетке 512×256;
- весь сопровождающий текст переписан под одну ветку результатов.

## 11. Раздел 7.4 — fly-by название «freeze6»
Подзаголовок переименован в «Стратегия дообучения с заморозкой процессора». Слово freeze6 оставлено как короткий идентификатор стратегии, но без `(freeze6)`-в-скобках и без bold.

## 12. Аналогии «катастрофическое забывание / Фаза 1 / Фаза 2»
Жирное снято. Аббревиатуры оставлены, но без выделения. Прибавлены связки «затем», «после» вместо нумерованных фаз.

## 13. Раздел 4 (усвоение данных)
- Жирное снято с: «анализ», «фоновое поле», «наблюдения», «субдискретизация», «инновация».
- Конструкция с `\underbrace{...}` для инновации заменена на обычное предложение с `\emph{}`.
- NaN-значения описаны как «пропуски»; «(ОИ)» в скобках убрано (термин раскрыт текстом).
- «Все 15 переменных» → «Все 19 переменных» (была ошибка счёта).

## 14. Раздел 5 (граничное сшивание)
Добавлен переходный абзац: сшивание Дэвиса — один из двух подходов к региональному прогнозу, второй — мультирезолюционная сетка (раздел 7). Пилотные результаты приведены в контексте, без обещаний.

## 15. Раздел 6.5 — единицы и числа
- «50000 / 150000 / 300000» → «50 / 150 / 300» (это километры, не метры).
- Жирное перенесено с 0.425 на 0.402 (правильное лучшее значение).

## 16. Раздел 7.1 (глобальный baseline)
Развёрнут перечень улучшений v2 (Swish вместо PReLU как более стабильная гладкая активация, увеличенная глубина процессора, AR-curriculum, остаточные связи). Технические замены описаны словами.

## 17. KD-tree
Расшифровано в тексте: «K-D-дерево — структура для быстрого поиска ближайших соседей в k-мерном пространстве». Расшифровка приведена в момент первого упоминания.

## 18. Сноска про Swish
Добавлена: `σ(x)·x`, где `σ` — сигмоида. Также пояснены остаточные связи (residual connection) — короткой фразой «прибавление входа к выходу слоя».

## 19. Раздел 8 (каскадный downscaler)
- Сняты bold на «Энкодер/Декодер/Входные каналы/Выходные каналы/Число параметров» в перечне.
- «1.67 → 1.69 °C» / «2.24 → 2.27 °C» переписано прозой.
- «значительно улучшает осадки» / «слабо ухудшает результат» / «селективно» — без bold.
- «корректирующая поправка (corrective residual)» → «поправка к coarse-полю».

## 20. Раздел 9 (постпроцессинг)
- Пайплайн с `\xrightarrow{GNN}…\xrightarrow{IDW}` заменён прозой («выход GNN последовательно проходит через поправку по вертикальному градиенту, затем Learned MOS… и в завершение IDW…»).
- Сняты bold на: «representativeness error», «Усреднение grid-box», «Urban Heat Island (UHI)», «Сезонная зависимость bias», «Красноярск», «вертикальному температурному градиенту», «Model Output Statistics (MOS)», «HistGradientBoostingRegressor», «Вектор признаков», «Inverse Distance Weighting», «Параметры:», «Итоговый порядок…», «1.32 °C», «1.40 °C», «1.02 м/с», «66 станций», «Реализация.», «мультипликативная сезонная коррекция», «Скорость и направление:», «Оценка порывов ветра», «Пространственное усреднение по зонам.», «Тип осадков».
- Английский дубль «representativeness error» убран — оставлено русское «ошибка репрезентативности».
- «Urban Heat Island (UHI)» → «городской тепловой остров»; «UHI» в дальнейшем — без скобок.
- Шаг сравнения «GNN raw → +lapse → +MOS → +IDW» переписан перечислением.

## 21. Раздел 10 (live-прогноз)
- В таблице 11 сняты bold на отклонениях `+3.9`, `+4.4`, `+5.5`, `+5.8` — это были рядовые ошибки, а не выделения «лучшего».
- В таблице 12 (MAE по суткам) bold оставлен только на минимуме MAE дня 3 и на «Итого» в строке Cascade+pp (это действительно лучшее значение). С остальных снято.
- «1.5 °C», «1.3 °C» — без bold.
- «4 суток» — без bold.

## 22. Заключение
- «fine-tune» → «дообучение» (lines 1970, 1975).
- Снято bold с: «57.3 %», «75.8 %», «1.40 °C», «2.5 раза», «75.16 %», «66.88 %», «84.08 %», «77.86 %», «87.52 %», «82.83 %», «1.5 °C», «4 суток».
- Из формулировки убраны двойные ссылки interp/merge — оставлена только merge-ветка с указанием на DA-эксперимент.
- Жирное сохранено только на структурных маркерах: «Основные результаты работы:», «Перспективы дальнейшей работы:».

## 23. Сплошной проход по тексту на остаточные проблемы
Прошёлся отдельно поверх 23 пунктов и поправил то, что было того же класса:
- остатки `multires_…` чекпоинтов в разделе 7.5 и в заключении;
- остатки `\textbf{…}` на парах «п.п.», «°C», «м/с» в перечислениях;
- остатки «(corrective residual)», «(Processor)», «(UHI)» в скобках;
- табличные `Interpolate / Merge` столбцы в табл. 8c (свёрнуто в один столбец);
- в подзаголовках разделов сняты bold-вставки внутри `\subsection{...}`;
- ссылка `(см. таблицы 8b, 8d)` в разделе 7.5 → `(раздел 7.6)`.

---

## Что не делал (специально):
- Не правил техническую лексику в коде, конфигах и README. Замены применены только к тексту диплома.
- В формулах архитектуры (`64 \to 128 \to 256 \to 512`) стрелки оставил — это математическая запись.
- Цифры по результатам не перепроверял по логам — все значения оставил как есть, правил только форму.

## Артефакты
- `docs/diploma/diploma.tex` — обновлённый исходник.
- `docs/diploma/diploma.pdf` — пересобран XeLaTeX, 53 страницы, 3 прохода (longtable требует ≥2).
- Остальные `.aux/.log/.toc/.out` — служебные, можно `.gitignore`-нуть.

Если что-то пропустил из 23 пунктов или приведённых выше категорий — кинь конкретное место (раздел + цитата строки), посмотрю точечно.
