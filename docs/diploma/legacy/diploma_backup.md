# МИНИСТЕРСТВО НАУКИ И ВЫСШЕГО ОБРАЗОВАНИЯ РОССИЙСКОЙ ФЕДЕРАЦИИ

## НОВОСИБИРСКИЙ ГОСУДАРСТВЕННЫЙ УНИВЕРСИТЕТ

**Факультет информационных технологий**  
**Кафедра информатики**

---

&nbsp;

&nbsp;

# ВЫПУСКНАЯ КВАЛИФИКАЦИОННАЯ РАБОТА

## ПОВЫШЕНИЕ ТОЧНОСТИ РЕГИОНАЛЬНОГО ПРОГНОЗА ПОГОДЫ В ГРАФОВОЙ НЕЙРОСЕТЕВОЙ МОДЕЛИ С ПОМОЩЬЮ УСВОЕНИЯ ДАННЫХ

&nbsp;

**УДК 519.6 : 551.5**

&nbsp;

**Выполнил:** студент __ курса, группы __  
Табаков Артур Станиславович

**Научный руководитель:**  
д.ф.-м.н., ВНС ИВМиМГ СО РАН  
Пененко Алексей Владимирович

&nbsp;

&nbsp;

---

**Новосибирск, 2026**

---

\newpage

## АННОТАЦИЯ

Настоящая работа посвящена разработке и исследованию методов повышения точности регионального прогноза погоды на основе графовой нейросетевой модели. В качестве основы используется архитектура, воспроизводящая ключевые элементы системы Google GraphCast: трёхфазный конвейер «кодировщик–процессор–декодировщик» на икосаэдральной мозаичной сетке. Модель обучена на данных реанализа ERA5 по 15–19 метеорологическим переменным для глобальной сетки 64×32 и регионального домена 61×41 с разрешением 0.25°.

В работе идентифицированы и исследованы две ключевые проблемы регионального нейросетевого прогнозирования: накопление ошибки в авторегрессионном режиме и появление артефактов на искусственных границах расчётной области. Для решения первой проблемы реализованы два алгоритма усвоения данных на этапе инференса: метод подталкивания (Nudging) и метод оптимальной интерполяции (OI). Для решения второй проблемы разработана процедура сшивания на основе двумерного окна Ханна.

По результатам численных экспериментов на тестовой выборке за период 2020 года (регион Новосибирской области, 53–57°с.ш., 79–85°в.д.) показано, что совместное применение оптимальной интерполяции и сшивания границ позволяет снизить RMSE с 0.629 до 0.414 (в нормированных единицах) и поднять skill-score с 19.7% до 47.2% относительно персистентного прогноза при горизонте 24 часа.

**Ключевые слова:** нейросетевой прогноз погоды, графовые нейронные сети, GraphCast, региональный прогноз, усвоение данных, Nudging, оптимальная интерполяция, ERA5, граничные условия.

---

\newpage

## СОДЕРЖАНИЕ

1. Введение
2. Обзор литературы
   - 2.1. Традиционные методы численного прогноза погоды
   - 2.2. Нейросетевые подходы к прогнозированию погоды
   - 2.3. GraphCast и архитектуры на основе GNN
   - 2.4. Методы усвоения данных в прогнозировании погоды
3. Описание модели
   - 3.1. Постановка задачи и входные данные
   - 3.2. Икосаэдральная мозаичная сетка
   - 3.3. Построение графов соединений
   - 3.4. Архитектура нейронной сети
   - 3.5. Функция потерь и обучение
4. Усвоение данных на этапе инференса
   - 4.1. Постановка задачи усвоения
   - 4.2. Метод подталкивания (Nudging)
   - 4.3. Оптимальная интерполяция
5. Обработка границ расчётной области
   - 5.1. Проблема искусственных границ
   - 5.2. Буферная зона с косинусным сглаживанием
6. Вычислительные эксперименты
   - 6.1. Данные и конфигурация экспериментов
   - 6.2. Метрики качества
   - 6.3. Результаты экспериментов
   - 6.4. Анализ влияния состава усваиваемых переменных
   - 6.5. Подбор гиперпараметров
7. Заключение
8. Список литературы

---

\newpage

## 1. ВВЕДЕНИЕ

Точный прогноз погоды является одной из наиболее важных и вместе с тем сложных задач современной науки. От качества прогнозирования зависит безопасность авиации, принятие сельскохозяйственных решений, планирование энергетики и управление чрезвычайными ситуациями. Традиционные методы численного прогноза погоды (Numerical Weather Prediction, NWP), основанные на численном интегрировании уравнений гидродинамики атмосферы, достигли высокого уровня точности за несколько десятилетий развития, однако требуют значительных суперкомпьютерных ресурсов.

В последние годы произошёл качественный прорыв в применении методов машинного обучения для прогнозирования погоды. Системы FourCastNet [7], Pangu-Weather [8], а в особенности Google GraphCast [1] продемонстрировали точность, сопоставимую или превосходящую оперативные системы ECMWF для ряда ключевых переменных, при несравнимо меньших вычислительных затратах на этапе прогноза.

Тем не менее большинство существующих нейросетевых систем ориентированы на глобальный прогноз погоды. Для регионального прогнозирования возникают специфические трудности, не характерные для глобального масштаба. Во-первых, при авторегрессионном продлении прогноза ошибки накапливаются со временем, что особенно критично для ограниченных областей. Во-вторых, на искусственных границах расчётного домена могут возникать численные артефакты, обусловленные несогласованностью начальных и граничных условий в нейросетевом формализме.

Настоящая работа посвящена решению этих двух проблем применительно к GNN-модели формата GraphCast, адаптированной для регионального прогноза в Западной Сибири (регион Новосибирской области). Для борьбы с накоплением ошибки предлагается интегрировать алгоритмы усвоения данных непосредственно в процедуру инференса модели, что позволяет периодически корректировать прогностическое состояние на основе доступных наблюдений. Для устранения граничных артефактов предлагается процедура плавного сшивания прогноза региональной модели с фоновыми данными глобального реанализа ERA5 с помощью двумерного окна Ханна.

**Цель работы** — исследовать применимость алгоритмов усвоения данных (Nudging и оптимальная интерполяция) и метода граничного сшивания для повышения точности авторегрессионного нейросетевого регионального прогноза погоды.

**Задачи работы:**
1. Реализовать архитектуру GNN-модели прогноза погоды, инспирированную GraphCast, для регионального домена.
2. Разработать и реализовать модули Nudging и оптимальной интерполяции для пост-шагового усвоения данных.
3. Разработать и реализовать процедуру сшивания на искусственных границах расчётной области.
4. Провести численные эксперименты, оценить вклад каждого метода в улучшение метрик RMSE и ACC.
5. Исследовать зависимость качества усвоения от набора усваиваемых переменных и гиперпараметров алгоритмов.

---

\newpage

## 2. ОБЗОР ЛИТЕРАТУРЫ

### 2.1. Традиционные методы численного прогноза погоды

Численный прогноз погоды (НПП) основан на численном решении системы уравнений в частных производных, описывающих динамику атмосферы: уравнений Навье–Стокса, уравнений термодинамики и переноса влаги. Современные оперативные системы, такие как IFS (ECMWF), GFS (NOAA) и ICON (DWD), используют сферические гармоники или конечные разности на структурированных сетках. Пространственное разрешение ведущих глобальных систем составляет 9–25 км, шаг по времени — несколько минут.

Вместе с численными схемами НПП неотделим от системы усвоения данных (Data Assimilation, DA), задача которой — совместить модельный прогноз с реальными наблюдениями. Методы DA варьируются от простейшей оптимальной интерполяции [2, 3] до вариационных систем (3D-Var, 4D-Var) и ансамблевого фильтра Калмана. Оперативные мировые центры используют 4D-Var или гибридные ансамблевые/вариационные схемы.

Для регионального масштаба оперативно применяются модели типа WRF, COSMO, AROME. Они работают в режиме «ограниченной области» (Limited Area Model, LAM) и требуют задания граничных условий от глобальной модели. Классический метод нанесения граничных условий — Davies relaxation [4] — реализует плавное сшивание поля LAM с полем глобальной модели в буферной зоне.

### 2.2. Нейросетевые подходы к прогнозированию погоды

Применение нейронных сетей для прогноза погоды началось с простых регрессионных моделей для статистического постпроцессинга [9]. Первый прорыв произошёл в 2022–2023 годах, когда возможности трансформерных архитектур и свёрточных нейросетей были применены непосредственно для мощного многовременного прогноза.

**FourCastNet** [7] — модель на основе адаптивного преобразования Фурье (AFNO), обучённая на данных ERA5 с разрешением 0.25°, демонстрирует высокую скорость вычислений при конкурентоспособной точности краткосрочного прогноза.

**Pangu-Weather** [8] — иерархическая трансформерная модель с 3D Earth Specific Transformer (3DEST), работающая на разных временных горизонтах с помощью набора отдельных моделей. Показывает высокую точность для горизонтов 1–7 суток.

**GraphCast** [1] компании Google DeepMind — GNN-модель на икосаэдральной сетке, обученная на ERA5 2010–2018. При прогнозе на 10 суток точнее IFS ECMWF по 90% из 1380 оцениваемых величин. Эта работа стала ключевым ориентиром для настоящего исследования.

### 2.3. GraphCast и архитектуры на основе GNN

Графовые нейронные сети (GNN) позволяют работать с нерегулярными сетками и естественным образом моделируют передачу информации между пространственно связанными узлами. В контексте прогноза погоды GNN привлекательны тем, что:
- позволяют использовать глобальную икосаэдральную мозаику, которая почти равномерно покрывает сферу, в отличие от географической сетки с неравномерным шагом по латитуде;
- обеспечивают масштабируемость через иерархию разрешений;
- могут эффективно обрабатывать нерегулярные области (региональный прогноз).

Архитектура GraphCast реализует трёхуровневую схему: **Encoder** (Grid→Mesh), **Processor** (Mesh↔Mesh) и **Decoder** (Mesh→Grid). Processor реализован как стек «Interaction Networks» [10]: на каждом шаге обновляются сначала признаки рёбер, затем — узлов с residual-соединениями. Такой подход имеет теоретическое обоснование как аппроксимация численного интегрирования дифференциальных уравнений через message passing [11].

### 2.4. Методы усвоения данных в прогнозировании погоды

**Nudging (метод подталкивания)** — один из наиболее ранних и практически применяемых методов усвоения данных в мезомасштабных моделях [5]. Основная идея состоит в добавлении к правой части уравнения прогноза члена, пропорционального разности между модельным полем и наблюдением:
$$\frac{\partial x}{\partial t} = F(x) + \alpha \cdot (x^{obs} - x)$$
где $\alpha$ — коэффициент подталкивания, $x^{obs}$ — наблюдаемое значение. В дискретной форме, применяемой в данной работе на этапе инференса:
$$x_{t+1}^{(new)} = x_{t+1} + \alpha (x_{t+1}^{obs} - x_{t+1})$$
Метод прост в реализации, требует лишь одного гиперпараметра ($\alpha$), но применяет правку только в точках наблюдений — пространственная корреляция ошибок не учитывается.

**Оптимальная интерполяция (OI)** [2] — классический метод статистического оценивания, минимизирующий дисперсию ошибки анализа. Алгоритм получает взвешенное сочетание прогноза и наблюдений с учётом пространственных корреляций ошибок:
$$x_a = x_b + K(y_o - Hx_b)$$
$$K = BH^T(HBH^T + R)^{-1}$$
где $x_b$ — фоновое поле (прогноз), $x_a$ — анализ, $y_o$ — наблюдения, $H$ — оператор наблюдений, $B$ — матрица ковариации ошибок прогноза, $R$ — матрица ковариации ошибок наблюдений, $K$ — матрица усиления Калмана. OI обеспечивает пространственное «размазывание» информации от редких станций на соседние узлы, что делает её значительно более мощным инструментом в сравнении с Nudging при разреженной сети наблюдений.

---

\newpage

## 3. ОПИСАНИЕ МОДЕЛИ

### 3.1. Постановка задачи и входные данные

Задача прогноза формулируется как авторегрессионное отображение: из $T_{obs}$ последовательных временных срезов состояния атмосферы предсказывается состояние на $T_{pred}$ шагов вперёд. Каждый временной срез задаётся полем из $C$ метеорологических переменных на регулярной координатной сетке.

В качестве обучающих данных использован реанализ ERA5 [6] от ECMWF за период 2010–2019 годов с временным шагом 6 часов и пространственным разрешением 0.25°. Набор из 15 динамических переменных включает:

| Переменная | Описание |
|:---|:---|
| *t2m* | Температура воздуха на высоте 2 м |
| *msl* | Давление на уровне моря |
| *10u*, *10v* | Компоненты ветра на высоте 10 м |
| *tp* | Суммарные осадки за 6 ч |
| *t*850, *t*500 | Температура на уровнях 850 и 500 гПа |
| *u*850, *u*500, *u*250 | Зональная компонента ветра (850/500/250 гПа) |
| *v*850, *v*500, *v*250 | Меридиональная компонента ветра (850/500/250 гПа) |
| *z*500, *z*850 | Геопотенциал (500/850 гПа) |

Кроме динамических переменных, каждый узел сетки снабжается **статическими признаками**: нормализованными значениями широты, долготы и их синус/косинус-преобразованиями, которые concatenate к входным признакам перед кодировщиком и не меняются при прогнозировании.

Для глобального эксперимента использовалась сетка 64×32 (2048 узлов, разрешение ~2.8°); для регионального эксперимента — сетка 61×41 (2501 узел, разрешение 0.25°, область 47–57.5°с.ш., 75–90°в.д.).

Конфигурация обучения: $T_{obs} = 4$ временных шага (24 ч истории), $T_{pred} = 4$ (прогноз +6, +12, +18, +24 ч).

### 3.2. Икосаэдральная мозаичная сетка

Для создания латентного пространства модели используется икосаэдральная мозаичная сетка (icosahedral mesh), обеспечивающая квазиравномерное покрытие сферы. Исходный икосаэдр (20 равносторонних треугольников, 12 вершин) последовательно рафинируется: на каждом уровне каждый треугольник делится на 4 подтреугольника путём вставки вершин в середины рёбер с последующей нормировкой на сферу. Уровень рафинирования $L$ даёт $10 \cdot 4^L + 2$ вершин.

В настоящей работе используется иерархия двух уровней сетки: $L = 3$ (642 вершины) и $L = 5$ (10 242 вершины). В процессоре модель оперирует на самом мелком уровне $L = 5$. Для регионального домена применяется **обрезка мозаики** до ограниченной области с буфером 15° по каждой стороне, что резко сокращает число узлов и вычислительные затраты по сравнению с глобальным графом.

### 3.3. Построение графов соединений

Модель использует три графа:

**Граф кодировщика (Grid→Mesh).** Каждый узел регулярной сетки соединяется с ближайшими mesh-узлами по радиусному критерию: радиус связи равен $r = q \cdot \ell_{max}$, где $\ell_{max}$ — длина самого длинного ребра мозаики на тонком уровне, $q = 0.6$ (параметр конфигурации). Это обеспечивает «закачку» информации из плотной географической сетки в разреженную мозаику.

**Граф процессора (Mesh→Mesh).** Граф строится по рёбрам мозаики $L = 5$. Каждое ребро снабжается **пространственными признаками** — 4-мерным вектором: расстояние между концами и их 3D-координаты в декартовой системе отсчёта. Эти признаки передаются в InteractionNet при обновлении рёбер.

**Граф декодировщика (Mesh→Grid).** Каждый узел географической сетки проецируется в треугольник мозаики, в котором он лежит, и получает входные сигналы от трёх вершин этого треугольника (барицентрическая связь).

### 3.4. Архитектура нейронной сети

Основной блок модели реализует конвейер ENCODER–PROCESSOR–DECODER.

**Кодировщик (Encoder).** Принимает конкатенацию динамических и статических признаков для всех $(N_{grid} + N_{mesh})$ узлов. Состоит из полносвязного перцептрона (два скрытых слоя по 128 нейронов, активация Swish, послойная нормализация) и слоя графовой свёртки GCN (два слоя по 128 нейронов). Выход — латентный вектор размерности 128 для каждого узла.

**Процессор (Processor).** Работает только на узлах мозаичной сетки ($N_{mesh}$). Представляет собой стек из 8 последовательных шагов Interaction Network с **неразделёнными весами** (как в оригинальном GraphCast). Каждый шаг:
1. **Обновление рёбер:** $e_{ij}' = \text{MLP}_{edge}([h_i \| h_j \| e_{ij}]) + e_{ij}$ — конкатенация признаков отправителя, получателя и текущего ребра, затем MLP и residual-соединение.
2. **Агрегация:** для каждого узла-получателя усредняются обновлённые признаки всех входящих рёбер.
3. **Обновление узлов:** $h_i' = \text{MLP}_{node}([h_i \| \overline{e}_i]) + h_i$ — конкатенация с агрегированными признаками рёбер, MLP и residual-соединение.
4. **LayerNorm** после каждого шага.

Скрытый размер 128, признаки рёбер в латентном пространстве размерности 4→128.

**Декодировщик (Decoder).** Получает конкатенацию grid-латентов (от кодировщика) и mesh-латентов (от процессора) и проводит обратное message passing (Mesh→Grid) через GCNConv. Финальный линейный слой проецирует в $C \cdot T_{pred}$ выходных каналов — предсказания на все горизонты сразу.

Итоговый граф вычислений описывается следующей последовательностью преобразований. Пусть $d = 128$ — размерность латентного пространства, $C_s$ — число статических признаков на узел. Тогда:

$$\mathbf{X}^{(0)} \in \mathbb{R}^{(G+M)\times(T_{obs}\cdot C + C_s)} \xrightarrow{\text{Encoder}} \mathbf{Z} \in \mathbb{R}^{(G+M)\times d}$$

$$\mathbf{Z}_{mesh} \in \mathbb{R}^{M\times d} \xrightarrow{\text{Processor}\times 8} \tilde{\mathbf{Z}}_{mesh} \in \mathbb{R}^{M\times d}$$

$$[\mathbf{Z}_{grid} \,\|\, \tilde{\mathbf{Z}}_{mesh}] \in \mathbb{R}^{(G+M)\times d} \xrightarrow{\text{Decoder}} \hat{\mathbf{X}} \in \mathbb{R}^{G\times (C\cdot T_{pred})}$$

где $\|$ обозначает конкатенацию матриц по оси узлов, а из выхода декодировщика берутся только первые $G$ строк, соответствующие узлам регулярной сетки.

### 3.5. Функция потерь и обучение

Модель обучается методом минимизации взвешенного MSE:

$$\mathcal{L} = \frac{1}{N \cdot C \cdot T} \sum_{n,g,c,t} w_g \, m_g \, \left(\hat{x}_{n,g,c,t} - x_{n,g,c,t}\right)^2$$

где $w_g = \cos(\varphi_g) / \overline{\cos(\varphi)}$ — широтные весовые коэффициенты (более крупные уменьшают вес у полюсов), $m_g$ — пространственная маска, обнуляющая буферную зону.

Для борьбы с накоплением ошибки при обучении применяется **авторегрессионный curriculum**: в первых эпохах модель предсказывает 1 шаг, затем число шагов rollout увеличивается до $T_{pred} = 4$. Оптимизатор — Adam с начальной скоростью обучения $3 \cdot 10^{-4}$, ранняя остановка по метрике на валидационной выборке (patience = 10 эпох).

---

\newpage

## 4. УСВОЕНИЕ ДАННЫХ НА ЭТАПЕ ИНФЕРЕНСА

### 4.1. Постановка задачи усвоения

При авторегрессионном применении региональной модели ошибки нарастают с каждым прогностическим шагом. Если в момент времени $t_k$ применить доступные наблюдения для коррекции модельного поля, накопленная ошибка будет снижена перед следующим шагом интегрирования. Формально задача усвоения состоит в нахождении **анализа** $x_a$, оптимально объединяющего прогностическое **фоновое поле** $x_b$ (выход модели) и **наблюдения** $y_o$.

В рамках настоящей работы усвоение выполняется на этапе инференса, между последовательными предсказаниями модели. Это соответствует схеме **оперативного цикла усвоения** при непрерывном прогнозе: предсказание → коррекция наблюдениями → подача скорректированного состояния на следующий шаг.

Для имитации сети наблюдений была использована **субдискретизация** поля ERA5: случайным образом отбирается 10% узлов сетки (250 из 2501); именно в этих «станциях» прогнозное поле сравнивается с реальными значениями ERA5. Вне узлов станций поле считается ненаблюдаемым ($\text{NaN}$). Были сформированы четыре набора наблюдений, отличающихся набором включённых переменных:

| Набор | Переменные | Физическая интерпретация |
|:---|:---|:---|
| Все переменные | все 15 | Эталонный сценарий |
| Приземные | *t2m, msl, 10u, 10v, tp* | Простая метеостанция |
| Динамические | *10u, 10v, u, v, z* (все уровни), *msl* | Динамические переменные |
| Температура | *t2m, t*850, *t*500 | Только температура |

### 4.2. Метод подталкивания (Nudging)

**Алгоритм.** Пусть $x_t \in \mathbb{R}^{G \times C}$ — прогностическое поле (G узлов, C переменных) после $t$-го шага модели, $y_t \in \mathbb{R}^{G \times C}$ — поле наблюдений с $\text{NaN}$ в ненаблюдаемых узлах. Тогда анализ вычисляется по формуле:

$$x_a(g, c) = \begin{cases}
x_b(g,c) + \alpha \bigl(y_o(g,c) - x_b(g,c)\bigr), & \text{если } y_o(g,c) \neq \text{NaN}, \\
x_b(g,c), & \text{иначе}.
\end{cases}$$

Параметр $\alpha \in [0, 1]$ управляет силой усвоения: $\alpha = 0$ — прогноз без коррекции, $\alpha = 1$ — полная замена прогноза наблюдением.

**Режим «Sequential Nudging».** В данной работе применяется последовательная коррекция: после каждого авторегрессионного шага ($t = 1, 2, 3, 4$) модельный выход корректируется на основе соответствующего среза наблюдений, после чего скорректированное состояние подаётся на следующий шаг.

**Схема алгоритма.** Процедура Sequential Nudging формально описывается следующим образом. Пусть $\mathcal{O} \subset \{1,\ldots,G\}$ — множество узлов наблюдений (ненулевых в $y_o$).

$$x_a(g, c) = \begin{cases} x_b(g,c) + \alpha\,\delta(g,c), & g \in \mathcal{O},\\ x_b(g,c), & g \notin \mathcal{O}, \end{cases} \quad \delta(g,c) = y_o(g,c) - x_b(g,c).$$

**Выбор $\alpha$.** В ходе экспериментов по подбору гиперпараметров было проверено $\alpha \in \{0.1, 0.5, 0.9, 1.3\}$. Оптимальное значение $\alpha = 0.5$ показало наилучший баланс между коррекцией ошибки и устойчивостью — см. раздел 6.5.

### 4.3. Оптимальная интерполяция

**Теория.** Оптимальная интерполяция (ОИ) является частным случаем метода BLUE (Best Linear Unbiased Estimator). Матрица усиления Калмана $K$ вычисляется аналитически и минимизирует дисперсию ошибки анализа:

$$K = B H^T (H B H^T + R)^{-1}$$

Поправка анализа к фону:
$$x_a = x_b + K \underbrace{(y_o - H x_b)}_{\text{инновация}}$$

**Матрица ковариации ошибок фона $B$.** Пространственные корреляции ошибок прогноза задаются гауссовской моделью:
$$B_{ij} = \sigma_b^2 \cdot \exp\left(-\frac{d_{ij}^2}{L^2}\right)$$
где $d_{ij}$ — расстояние по поверхности Земли между узлами $i$ и $j$ (в метрах по формуле гаверсинуса), $L$ — радиус корреляции (гиперпараметр), $\sigma_b$ — стандартное отклонение ошибки фона.

**Физический смысл.** Если в точке $A$ прогноз отклонился от наблюдения на $\delta$, то ОИ «размазывает» эту поправку на соседние узлы с весом, убывающим с увеличением расстояния. Пространственный масштаб убывания определяется параметром $L$.

**Матрица ошибок наблюдений $R$.** Принята диагональной: $R = \sigma_o^2 \cdot I_{N_{obs}}$, что соответствует предположению о независимости ошибок разных наблюдений.

**Оператор наблюдений $H$.** Матрица $H \in \{0,1\}^{N_{obs} \times G}$ — билинейный оператор проекции: для каждого наблюдения находится ближайший узел сетки и соответствующий элемент $H$ устанавливается в 1.

**Вычислительная схема.** Матрица $B \in \mathbb{R}^{G \times G}$ строится единожды при инициализации алгоритма. Для каждой переменной $c = 1,\ldots,C$ на каждом шаге коррекции выполняется:

$$K^{(c)} = B H^{(c)\,T}\bigl(H^{(c)} B H^{(c)\,T} + R^{(c)} + \varepsilon I\bigr)^{-1}$$

$$x_a^{(c)} = x_b^{(c)} + K^{(c)}\underbrace{\bigl(y_o^{(c)} - H^{(c)} x_b^{(c)}\bigr)}_{\text{инновация}}$$

где $\varepsilon = 10^{-5}$ — параметр регуляризации, гарантирующий невырожденность обращаемой матрицы. Оператор наблюдений $H^{(c)}$ строится для каждой переменной независимо: ненулевые строки соответствуют наблюдаемым узлам, матрица $R^{(c)} = \sigma_o^2 I_{N_{obs}}$.

**Параметры.** Ключевые гиперпараметры ОИ: $\sigma_b$ (фиксирован = 0.8), $\sigma_o$ (подбирался из $\{0.2, 0.5, 0.8\}$), $L$ (подбирался из $\{50, 150, 300\}$ тыс. км). Матрица $B$ одинакова для всех переменных, поэтому коррекция применяется независимо по каждому каналу $c$.

---

\newpage

## 5. ОБРАБОТКА ГРАНИЦ РАСЧЁТНОЙ ОБЛАСТИ

### 5.1. Проблема искусственных границ

При региональном прогнозировании граница расчётного домена является **искусственной**: на ней отсутствуют физические условия, а информационный поток из внешней области обрывается. Это приводит к нарастанию ошибок у границ и отражению волн, которые в реальности должны выходить из домена (проблема «radiation condition»).

В рамках нейросетевого подхода проблема границ проявляется ещё резче: модель обучена на глобальных или иных данных, но применяется к вырезанной области — и граничные узлы никогда не «видят» правильного внешнего окружения.

Классическое решение в задачах NWP — метод Davies [4]: в буферной зоне шириной $b$ узлов прогноз региональной модели $x_{LAM}$ плавно смешивается с данными глобальной модели $x_{bg}$:

$$x = w \cdot x_{LAM} + (1 - w) \cdot x_{bg}$$

где весовая функция $w$ монотонно растёт от 0 у внешней границы до 1 в центре домена.

### 5.2. Буферная зона с косинусным сглаживанием

В данной работе реализовано двумерное **окно Ханна** (Hann window, cosine taper):

$$w_{lon}(i) = \begin{cases} \frac{1}{2}\left[1 - \cos\!\left(\frac{\pi i}{b}\right)\right], & 0 \le i < b, \\ 1, & b \le i < N_{lon} - b, \\ \frac{1}{2}\left[1 - \cos\!\left(\frac{\pi (N_{lon}-1-i)}{b}\right)\right], & N_{lon} - b \le i < N_{lon} \end{cases}$$

и аналогично $w_{lat}(j)$. Итоговая двумерная маска:
$$W(i, j) = w_{lon}(i) \cdot w_{lat}(j)$$

**Выбор косинусного окна вместо линейного.** Косинусное окно предпочтительно по ряду причин:
1. Производная $W$ обращается в нуль на обоих концах буферной зоны — обеспечивается $C^1$-гладкость перехода, что важно для стабильности нейросетевого предсказания на следующем шаге.
2. Спектральные утечки при косинусном тейпере значительно меньше, чем при линейном — это уменьшает возникновение высокочастотных артефактов у границ.
3. Косинусное окно соответствует принципу максимально плавного сопряжения полей, изложенному в работе Davies [4].

**Процедура применения.** На каждом шаге авторегрессионного прогноза:
$$x_{out}(g) = W(g) \cdot x_{LAM}(g) + (1 - W(g)) \cdot x_{bg}(g)$$

где $x_{bg}$ — фоновое поле (в экспериментах — данные ERA5, в оперативном режиме — прогноз глобальной модели). Значение маски $W(g) \in [0, 1]$ плоского массива узлов вычисляется по заранее построенному двумерному окну Ханна с шириной буфера $b = 5$ узлов.

---

\newpage

## 6. ВЫЧИСЛИТЕЛЬНЫЕ ЭКСПЕРИМЕНТЫ

### 6.1. Данные и конфигурация экспериментов

Модель обучалась на данных ERA5 за период 2010–2019 гг. (тренировочная выборка), 2020 первое полугодие (валидация), второе полугодие 2020 года (тестирование). Конфигурация использованной модели:

| Параметр | Значение |
|:---|:---|
| Область | 47–57.5°с.ш., 75–90°в.д. (Западная Сибирь) |
| Сетка | 61×41, разрешение 0.25° |
| Переменных | 15 динамических |
| $T_{obs}$ | 4 шага (24 ч предыстории) |
| $T_{pred}$ | 4 шага (+6, +12, +18, +24 ч) |
| Уровни мозаики | L=3, L=5 |
| Размерность латента | 128 |
| Шагов процессора | 8 |
| Эпо̀х обучения | 50 |
| Оптимизатор | Adam, lr=$3\cdot10^{-4}$ |

Тестовая выборка содержала 13 независимых образцов. Для оценки усвоения данных использовалась сеть из 250 «станций» (10% узлов сетки), выбранных случайно, но фиксированных для всех экспериментов. Наблюдения для усвоения — значения ERA5 на этих узлах (идеализированный эксперимент).

### 6.2. Метрики качества

Для оценки прогнозов использовались стандартные метеорологические метрики:

**RMSE** (Root Mean Squared Error) — в нормированных единицах (после стандартизации по каналу):
$$\text{RMSE} = \sqrt{\frac{1}{G \cdot C} \sum_{g,c} \left(\hat{x}_{g,c} - x_{g,c}\right)^2}$$

**Skill Score** — относительное улучшение по отношению к **персистентному** прогнозу (прогнозу «без изменений»):
$$\text{Skill} = 1 - \frac{\text{RMSE}_{model}}{\text{RMSE}_{persistence}}$$

**ACC** (Anomaly Correlation Coefficient) — пространственная корреляция аномалий:
$$\text{ACC} = \frac{\sum_{g,c} (x_{g,c} - \bar{x}_c)(\hat{x}_{g,c} - \bar{\hat{x}}_c)}{\sqrt{\sum_{g,c}(x_{g,c}-\bar{x}_c)^2 \cdot \sum_{g,c}(\hat{x}_{g,c}-\bar{\hat{x}}_c)^2}}$$

Все метрики вычислялись как для полного домена (61×41), так и для центрального подрегиона Новосибирской области (53–57°с.ш., 79–85°в.д., 425 узлов).

### 6.3. Результаты экспериментов

Таблица 1 суммирует результаты основных конфигураций на горизонте +24 ч для полного домена.

**Таблица 1.** Метрики качества на горизонте +24 ч, полный домен 61×41.

| Конфигурация | RMSE↓ | Skill↑ | ACC↑ |
|:---|:---:|:---:|:---:|
| Персистентность (baseline) | 0.783 | 0% | 0.377 |
| **Базовая модель (control)** | 0.691 | 19.7% | 0.273 |
| Только границы (bounds, $b=5$) | 0.589 | 32.2% | 0.459 |
| Nudging, все переменные ($\alpha=0.5$) | 0.664 | 23.5% | 0.316 |
| Nudging + границы | 0.562 | 35.2% | 0.494 |
| ОИ, все перем. ($L=300$ км, $\sigma_o=0.5$) | 0.538 | 38.0% | 0.493 |
| **ОИ + границы** | **0.430** | **50.4%** | **0.649** |

Таблица 2 — результаты для регионального среза (Новосибирская область, +24 ч).

**Таблица 2.** Метрики качества на горизонте +24 ч, регион НСКО (53–57°с.ш., 79–85°в.д.).

| Конфигурация | RMSE↓ | Skill↑ | ACC↑ |
|:---|:---:|:---:|:---:|
| Персистентность (baseline) | 0.660 | 0% | 0.204 |
| **Базовая модель (control)** | 0.621 | 15.4% | 0.170 |
| Только границы | 0.621 | 15.4% | 0.170 |
| Nudging, все переменные ($\alpha=0.5$) | 0.598 | 18.5% | 0.198 |
| ОИ, все перем. ($L=300$ км, $\sigma_o=0.5$) | **0.404** | **44.9%** | **0.306** |

**Анализ результатов.** Наиболее значимые наблюдения:

1. **Сшивание границ** само по себе даёт существенный прирост на полном домене (Skill 19.7% → 32.2%) — основной вклад идёт от исправления ошибок у краёв расчётной области. На центральном подрегионе (НСКО) этот эффект менее выражен — Skill практически не меняется, что подтверждает локализацию граничных артефактов вблизи краёв домена.

2. **Nudging** даёт умеренное улучшение на обоих доменах. Его эффект сильнее при совместном использовании со сшиванием границ.

3. **ОИ** превосходит Nudging как на полном домене, так и особенно на центральном подрегионе (RMSE 0.621 → 0.404, Skill 15.4% → 44.9%). Это объясняется способностью ОИ распространять информацию от немногочисленных наблюдений на соседние узлы, тогда как Nudging напрямую корректирует только наблюдаемые узлы.

4. **Комбинация ОИ + границы** на полном домене даёт наилучший результат: Skill 50.4%, ACC = 0.649 — значительное превышение всех остальных конфигураций.

### 6.4. Анализ влияния состава усваиваемых переменных

Таблица 3 показывает влияние выбора набора усваиваемых переменных (метод Nudging, $\alpha=0.5$, полный домен, +24 ч).

**Таблица 3.** Зависимость RMSE от набора усваиваемых переменных (Nudging, +24 ч).

| Набор | Переменные | RMSE↓ | Skill↑ |
|:---|:---|:---:|:---:|
| Все 15 переменных | все | 0.664 | 23.5% |
| Динамические | ветер, геопотенциал | 0.675 | 22.3% |
| Приземные | t2m, msl, ветер | 0.680 | 21.6% |
| Только температура | t2m, t850, t500 | 0.691 | 19.7% |

Усвоение только температуры практически не улучшает прогноз. Причина — атмосферная динамика: если прогнозируемый ветер неверен, скорректированная температура быстро «исправится» обратно динамическими процессами за 1–2 шага. Для ОИ наблюдается та же тенденция: наилучшие результаты при усвоении всех или динамических переменных.

### 6.5. Подбор гиперпараметров

**Nudging — выбор $\alpha$.** Эксперименты с $\alpha \in \{0.1, 0.5, 0.9, 1.3\}$ показали, что оптимальным является $\alpha = 0.5$. При $\alpha > 0.9$ модель «перешагивает» и вносит высокочастотные аномалии в поле, при $\alpha < 0.3$ эффект коррекции слишком мал.

**ОИ — выбор $L$ и $\sigma_o$.** Результаты поиска по сетке приведены в таблице 4 (RMSE для региона НСКО, +24 ч).

**Таблица 4.** Подбор параметров ОИ: RMSE региона при горизонте +24 ч.

| $L$, км | $\sigma_o = 0.2$ | $\sigma_o = 0.5$ | $\sigma_o = 0.8$ |
|:---:|:---:|:---:|:---:|
| 50 000 | 0.534 | 0.499 | 0.497 |
| 150 000 | 0.507 | 0.443 | **0.425** |
| 300 000 | 0.414 | 0.404 | 0.402 |

Лучшие результаты достигаются при $L = 300$ км и $\sigma_o = 0.5$–$0.8$. Большой радиус корреляции обоснован тем, что реальные пространственные корреляции ошибок нейросетевой модели имеют крупный масштаб (несколько сотен километров), превышающий таковой для традиционного NWP.

**Ширина буфера $b$.** Тесты с $b \in \{3, 5, 7\}$ показали, что $b = 5$ узлов (≈ 125 км) выгодно балансирует между шириной зоны сопряжения и потерей «рабочей» площади прогноза.

---

\newpage

## 7. ЗАКЛЮЧЕНИЕ

В настоящей работе разработан и исследован комплекс методов повышения точности регионального нейросетевого прогноза погоды на основе GNN-архитектуры, воспроизводящей принципы Google GraphCast.

**Основные результаты работы:**

1. Реализована и обучена региональная GNN-модель прогноза погоды для Западной Сибири, обеспечивающая Skill Score 19.7% относительно персистентного прогноза при горизонте +24 ч — что значительно превосходит базовый уровень без применения нейросетей.

2. Разработан и реализован модуль **Nudging** (метода подталкивания) для последовательной авторегрессионной коррекции прогноза с возможностью гибкой настройки набора усваиваемых переменных. Оптимальный коэффициент $\alpha = 0.5$; применение усвоения всех переменных улучшает Skill на 3.8 п.п. (при работе без сшивания границ).

3. Разработан и реализован модуль **Оптимальной интерполяции** с гауссовской моделью ковариации ошибок фона. Метод позволяет пространственно распространять информацию от редких наблюдений и демонстрирует значительно лучшие результаты, чем Nudging: Skill 44.9% vs 18.5% для центрального подрегиона. Оптимальные параметры: $L = 300$ тыс. км, $\sigma_o = 0.5$–$0.8$.

4. Разработана и реализована процедура сшивания на искусственных границах расчётной области с помощью двумерного окна Ханна (шириной буфера $b = 5$ узлов). Обоснован выбор косинусного тейпера как оптимального с точки зрения гладкости и минимизации спектральных артефактов. Применение сшивания само по себе улучшает Skill на полном домене с 19.7% до 32.2%.

5. Показано, что **комбинация ОИ + граничное сшивание** обеспечивает наилучший результат: RMSE = 0.430 (vs 0.691 для базовой модели), Skill = 50.4%, ACC = 0.649 — при горизонте +24 ч на полном домене 61×41. Это свидетельствует о синергическом эффекте двух методов: граничное сшивание уменьшает ошибки у краёв, а ОИ снижает ошибку в ядре домена.

6. Установлено, что за состав усваиваемых переменных важна динамика (ветер, геопотенциал): усвоение одной лишь температуры практически неэффективно из-за быстрого перераспределения тепла динамическими процессами.

**Перспективы дальнейшей работы:**
- Переход к 4D-Var или ансамблевому фильтру Калмана (EnKF) для более строгого усвоения;
- интеграция усвоения внутрь обучения (онлайн-DA), а не только на этапе инференса;
- применение реальных данных метеостанций вместо субдискретизации ERA5;
- исследование влияния числа «станций» и их пространственного распределения на качество усвоения.

---

\newpage

## СПИСОК ЛИТЕРАТУРЫ

1. Lam R., Sanchez-Gonzalez A., Willson M. et al. Learning skillful medium-range global weather forecasting // Science. 2023. Vol. 382, No. 6677. P. 1416–1421.

2. Gandin L. S. Objective Analysis of Meteorological Fields. Leningrad: Gidrometeoizdat, 1963. (перевод: Israel Program for Scientific Translations, Jerusalem, 1965.)

3. Lorenc A. C. A global three-dimensional multivariate statistical interpolation scheme // Monthly Weather Review. 1981. Vol. 109, No. 4. P. 701–721.

4. Davies H. C. A lateral boundary formulation for multi-level prediction models // Quarterly Journal of the Royal Meteorological Society. 1976. Vol. 102, No. 432. P. 405–418.

5. Stauffer D. R., Seaman N. L. Use of four-dimensional data assimilation in a limited-area mesoscale model. Part I: Experiments with synoptic-scale data // Monthly Weather Review. 1990. Vol. 118, No. 6. P. 1250–1277.

6. Hersbach H., Bell B., Berrisford P. et al. The ERA5 global reanalysis // Quarterly Journal of the Royal Meteorological Society. 2020. Vol. 146, No. 730. P. 1999–2049.

7. Pathak J., Subramanian S., Harrington P. et al. FourCastNet: A global data-driven high-resolution weather model using adaptive Fourier neural operators // arXiv:2202.11214, 2022.

8. Bi K., Xie L., Zhang H., Chen X., Gu X., Tian Q. Accurate medium-range global weather forecasting with 3D neural networks // Nature. 2023. Vol. 619. P. 533–538.

9. Pfaff T., Fortunato M., Sanchez-Gonzalez A., Battaglia P. Learning mesh-based simulation with graph networks // International Conference on Learning Representations (ICLR). 2021.

10. Battaglia P. W., Hamrick J. B., Bapst V. et al. Relational inductive biases, deep learning, and graph networks // arXiv:1806.01261, 2018.

11. Sanchez-Gonzalez A., Godwin J., Pfaff T. et al. Learning to simulate complex physics with graph networks // International Conference on Machine Learning (ICML). 2020. P. 8459–8468.

12. Kalnay E. Atmospheric Modeling, Data Assimilation and Predictability. Cambridge University Press, 2003.

13. Dee D. P., Uppala S. M., Simmons A. J. et al. The ERA-Interim reanalysis: Configuration and performance of the data assimilation system // Quarterly Journal of the Royal Meteorological Society. 2011. Vol. 137, No. 656. P. 553–597.

14. Sela J. G. The NMC spectral model // NOAA Technical Report NWS 30.1980.

15. Veličković P., Cucurull G., Casanova A. et al. Graph Attention Networks // International Conference on Learning Representations (ICLR). 2018.

---

*Работа выполнена в Новосибирском государственном университете при поддержке ИВМиМГ СО РАН.*
