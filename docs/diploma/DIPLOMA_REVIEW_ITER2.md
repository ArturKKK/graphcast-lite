# Ответы по итерации 2 (15 пунктов)

PDF пересобран: 52 страницы.

## 1. Оглавление: «Полный каскад GNN→UNet» (8.4)
Заголовок подраздела 8.4 и название Таблицы 10 переписаны без стрелки:
«Полный каскад GNN и UNet». В оглавлении теперь нет стрелок.

## 2. Длинные тире `---`
Глобально заменены на короткие тире `--` (en-dash). В теле документа
em-dash больше нет, только в LaTeX-комментариях разделителей.

## 3. Таблицы 1 и 2 (6.3): `(control)` и жирность
- Убрано слово `(control)` из строк «Базовая модель».
- Удалена жирность в строке «Базовая модель» и в строке «ОИ + границы»
  (заголовок строки), числа лучшей конфигурации в столбцах оставлены
  жирными — стандартная научная практика.

## 4. Стрелки в прозе 6.3
Заменены на словесное «с X до Y» в текстах об эффекте Сшивания границ и
ОИ.

## 5. 7.2 «удаляются», «133 279», «мультирезолюционный подход», 7.3
«жёсткой склейкой», 6.2 «персистентному», 6.4 RMSE/Skill/ACC заголовки —
все жирные начертания удалены.

## 6. 7.4 «freeze6 / nofreeze»
Термины оставлены только в качестве названий конфигураций в Таблице 6;
в прозе термин «freeze6» заменён на «фриз процессора»/«заморозка
процессора». Названия экспериментов из кодовой базы из текста удалены.

## 7. Таблица 7 (7.5)
Жирность 74.8 % и 73.9 % убрана (поясняющих столбцов лучше-хуже нет).

## 8. 7.5 — числа в физических единицах и пояснительные пометки
Удалена жирность с «1.40 °C» и со ссылочных «73.9 %», «75.16 %»,
«1.40 °C», «1.80 °C»; абзац-замечание о различных геометриях оценки
больше не имеет жирной подсветки.

## 9. WRF vs ERA5 / 1.40 °C
- WRF-таблица в 7.5 сохранена, но добавлено явное методологическое
  пояснение: оба прогноза оцениваются относительно ERA5 (WRF имеет
  собственную динамику и не настроен на воспроизведение ERA5); среднее
  WRF — по одному эпизоду (январь 2023), GraphCast-lite — по 27 случаям.
  Сравнение охарактеризовано как иллюстративное.
- По данным `aaaa/wrf_krasnoyarsk/compare_dworf_era5.md` числа в таблице
  диплома (1.439 / 1.788 / 0.451 / 0.586 / 0.514 / 0.910 / 4.576 / 0.834)
  взяты из строки «SUMMARY TABLE (for thesis)» лога; они верны.
- По числу «1.40 °C при +24 ч» (Таблица 6 и подраздел 7.5): значение
  получено для модели `multires_real_freeze6` на merge-датасете в
  внутренней зоне 45 узлов. В обучающем логе самого эксперимента
  непосредственного evaluation-вывода с per-horizon t2m RMSE не нашёл,
  поэтому самостоятельно подтвердить эту цифру без повторной валидации
  не могу. Для эксперимента `multires_nores_freeze6` (без merge,
  использовался для оценки 7-суточного авторегрессионного прогноза)
  +24 ч t2m RMSE ≈ 1.87 °C, а среднее по +6…+24 ч ≈ 1.44 °C. Если ты
  хочешь, чтобы я заменил «1.40 °C» на «1.44 °C (среднее +6…+24 ч)» или
  пересчитал на actual eval, скажи — переделаю.

## 10. Прочие стрелки
Заменены: «Grid→Mesh/Mesh↔Mesh/Mesh→Grid» в архитектуре; «1→2→3 шага»
описано словами. В формулах LaTeX (`\to`, `\xrightarrow`) стрелки
оставлены — это математическая нотация.

## 11. Терминология «merge», «freeze6»
- Заголовок 7.6 переписан: «Дообучение на композитном датасете с
  остаточной связью».
- «freeze6» в прозе 7.4 заменено на «заморозку процессора»; в Таблице 6
  оставлено как короткий тег конфигурации.

## 12. 9.3 — расширение MOS на Россию
Параграф «Расширение MOS на территорию России» удалён полностью; ссылки
на `learned_mos_russia_66st.joblib` и `live_runtime_bundle/` из текста
убраны. В заключении упоминание расширенного 66-станционного MOS также
удалено — теперь говорится только о 19 станциях Красноярского края.

## 13. Имена файлов кодовой базы
Удалены: `live_gdas_forecast.py`, `learned_mos_russia_66st.joblib`,
`live_runtime_bundle/`, `multires_nores_freeze6`. Тексты, где они
упоминались, переписаны нейтрально («оперативный live-пайплайн», «тот же
мультирезолюционный чекпоинт, что и в разделе 7.4»).

## 14. Постпроцессинг — пример живого результата
Раздел 10 уже содержит реальную верификацию по Таблицам 11 и 12
(3–6 апреля 2026). Дополнительного контента не добавлял.

## 15. Open-Meteo
В подразделе 10.2 уточнено, что Open-Meteo предоставляет ERA5-T
(оперативное продолжение реанализа ERA5), а не станционные наблюдения;
данные используются как proxy при недоступности прямых ISD-Lite-записей.

## Что остаётся открытым

- Если потребуется подтвердить «1.40 °C» по log_last.log самого
  `multires_real_freeze6`, нужен запуск predict с `--region` и сохранение
  per-horizon метрик; в репозитории такого артефакта пока нет.
- WRF-сравнение по-прежнему методологически слабое (1 эпизод против 27
  и проверка по ERA5). Если хочется усилить, можно либо снять WRF из
  диплома, либо добавить сравнение обоих по станционному ISD WMO 29570.
