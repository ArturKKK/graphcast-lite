# Реестр обученных моделей

Кто, когда, каким кодом и на каких данных обучен. Заполняется **при каждом
обучении**, иначе через месяц связь теряется — так уже вышло с ранними версиями.

---

## Правила (простые, без блокировок)

1. **Перед запуском обучения — закоммититься.** С версии от 07.08.2026
   `src/train.py` сам пишет в начало `training_log.txt` хеш коммита, md5 конфига
   и метаданные датасета. Если в момент запуска есть незакоммиченные изменения,
   в логе появится пометка `(есть незакоммиченные изменения!)` — это значит,
   что модель невоспроизводима, и такую строку в реестр вносить нельзя.

2. **После обучения — строка в этот файл** и `git add -f` весов, если модель
   рабочая. Веса в `.gitignore`, поэтому только через `-f` и осознанно: класть
   в репозиторий стоит те чекпойнты, на которых держатся публикуемые числа.

3. **Тег на коммит обучения:** `git tag model/<имя> <коммит>` и `git push --tags`.
   Тогда `git checkout model/krsk-33f` разворачивает ровно тот код.

4. **Датасет описывать по имени и параметрам**, а не «тот, что был на VM»:
   каталоги `/data` стираются при каждом перезапуске виртуалки.

5. **Ветка `main-arthur` рабочая, пушат в неё все машины.** Перед пушем всегда
   `git pull --rebase origin main-arthur`, иначе отказ по несовпадению истории.
   Формальные блокировки не нужны, достаточно этой привычки.

---

## Модели

### `multires_krsk_33f` — ОСНОВНАЯ МОДЕЛЬ СТАТЬИ

| | |
|---|---|
| Что | Красноярск, мультимасштабный граф, 33 канала |
| Обучение начато | 2026-07-25 17:03 |
| Коммит | **`d29519a`, установлен точно** (тег `model/krsk-33f`) — см. обоснование ниже |
| Дообучена от | `wb2_512x256_33f_ar_v3/best_model.pth` |
| Датасет | `multires_krsk_33f` (133 279 узлов, 16 072 срока, 33 канала); собирается `scripts/build_multires_russia_33f.py` из merge + global_extra + region_krsk_extra |
| Расписание | 32 эпохи, max_ar=4, заморозка процессора 6 эпох, lr 1e-4, множитель 0.1, сид 42, остаточная формулировка |
| Остановлено | эпоха 29 (по терпению) |
| Веса | `best_model.pth` md5 `1f4b64aad93f…` — эпоха 15, лучшая по val<br>`checkpoint.pth` md5 `2c7efc7217f4…` — **эпоха 28 (счёт с нуля), используется для всех числел статьи** |
| Качество | t2m 1,32 / 1,53 / 1,59 / 1,66 °C на +6…+24 ч, успешность 75,3 % (полный тест, 1607 сроков) |

Внимание: публикуемые числа получены на **последней** эпохе, а не на «лучшей по
val» — см. п. 4.4 статьи о ненадёжности одношагового критерия отбора.

**Как установлен коммит (11.08.2026).** Обучение шло до введения провенанса,
поэтому хеша в логе нет. Восстановлен по `git reflog` локальной копии, где
велась разработка:

| Событие | Время (+03:00) |
|---|---|
| коммит `edf837a` (setup_vm: coords.npz для сборщика 33f) | 16:40:04 |
| коммит `d29519a` (добавлен `scripts/paper_tables.py`) | 16:44:18 |
| **старт обучения** (`training_log.txt`) | **17:03:28** |
| коммит `6d48c32` (раннер M1+M5) | 19:36:38 |

Старт попадает в промежуток между `d29519a` и `6d48c32`, то есть HEAD был на
`d29519a`. Пояса совпадают: скрипты прогонов на виртуалке пишут `+03:00`, reflog
тоже `+03:00`.

Привязка устойчива к неточности на несколько минут: `edf837a` и `d29519a`
отличаются **только добавлением `scripts/paper_tables.py`** — скрипта разбора
логов в таблицы, который к обучению отношения не имеет. Так что даже если
виртуалка не подтянула последний коммит, обучающий код был тот же.

### `wb2_512x256_33f_ar_v3` — глобальная v3, отправная точка

| | |
|---|---|
| Обучение начато | 2026-05-11 09:47 |
| Коммит | не установлен; ближайшие по смыслу — `9ffdaa2` и `3af906c` (регистрация `dataset_name` v3 и параметров шума/весов каналов), но метки времени логов и коммитов в разных поясах, поэтому это догадка |
| Датасет | `wb2_512x256_33f_v3` (512×256, 33 канала) |
| Расписание | 80 эпох, max_ar=8, остановлено на 36, лучшая эпоха 30 |
| Веса | md5 `22397f67ef83…` |
| Качество | против v2 на общих каналах: t2m 1,07 против 1,32 °C, успешность 65,4 против 60,2 % (200 сроков, глобальная сетка) |

### `multires_merge_freeze6_v2` — региональная 19f, флагман прежней линии

| | |
|---|---|
| Обучение начато | 2026-04-16 23:38 |
| Коммит | не установлен |
| Датасет | `multires_krsk_19f_merge` |
| Расписание | 48 эпох, max_ar=4, заморозка 10 эпох, lr 5e-5, множитель 0.05 |
| Веса | md5 `6ad5ade3178d…` (на VM; в репозитории нет) |
| Качество | t2m 1,39 / 1,66 / 1,74 / 1,84 °C, успешность 70,3 % |

### `multires_nores_freeze6` и `multires_nores_nofreeze` — абляционная пара

Без остаточной формулировки, обучены на interp-сборке `multires_krsk_19f`,
32 эпохи, заморозка 6 эпох и 0 эпох соответственно. Используются только в
разделе 4.4 для сравнения стратегий дообучения при равном бюджете.
Веса md5 `c42ba49fa635…` и `bd518528f5b3…`.

### `wb2_512x256_19f_ar_v2` — глобальная v2

Отправная точка 19-канальной линии. Веса md5 `1a841c3c9420…`, лежат в
репозитории. Обучена с `use_residual=false` — при инференсе **обязателен флаг
`--no-residual`**, иначе метрики выходят бессмысленными (на этом уже дважды
падали прогоны).

### `multires_russia_33f_v3_noroi` — модель оперативного контура

Вся Россия, 33 канала, обучение начато 2026-05-14 22:57. Используется в
`scripts/cron_forecast_russia_v3.sh` вместе с `neural_postproc_v3`. **Это не
модель статьи** — другой домен и другой состав каналов.

### `multires_krsk_33f_frozen` — абляция: процессор заморожен всё обучение

| | |
|---|---|
| Обучение начато | 2026-08-07 12:22 |
| Коммит | `91b98af` |
| Запуск | `bash scripts/_paper_run_frozen.sh` |
| Отличие от основной | `freeze_processor_epochs = 32` (равно `num_epochs`) — учатся только кодировщик и декодировщик |
| Датасет | `multires_krsk_33f` (тот же) |
| Результат | лучший val 0,01918 против 0,01796 у основной, то есть **+7,5 %** при равном бюджете; выход на плато к 8-й эпохе |
| Разбор | `docs/ablations_2026-08.md` |

Вывод: региональная адаптация процессора необходима, обучением одних интерфейсов
не обойтись.

### `multires_krsk_33f_multimesh` — абляция: полный меш 0–6 (НЕ ЗАВЕРШЕНА)

| | |
|---|---|
| Обучение начато | 2026-08-07 12:22 |
| Коммит | `d03d200` (с незакоммиченным `config.json.bak` — провенанс запятнан) |
| Запуск | `bash scripts/_paper_run_multimesh.sh` |
| Отличие от основной | `graph.mesh_levels = [0..6]` вместо `[4, 6]` |
| Статус | **прервано на 17-й эпохе**, вероятно нехватка памяти при переходе на AR=3 (на карте шло второе обучение) |
| Оговорка | стартовая ошибка 0,21405 против 0,02091 — веса глобальной v3 обучались на другом наборе рёбер; сравнение некорректно |
| Разбор | `docs/ablations_2026-08.md` |

Выводов не делать: эксперимент требует либо предобучения глобальной модели с тем
же мешем, либо заметно большего бюджета дообучения.

---

## Что делать со старыми моделями

Точную привязку к коммитам для всего, что обучено до 07.08.2026, восстановить
уже нельзя. Заявлять её в статье не следует. Для воспроизводимости достаточно
того, что есть: конфиг, датасет с параметрами сборки, расписание, md5 весов и
метрики на фиксированном окне.
